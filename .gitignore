node_modules/
dist/
src-tauri/target/
src-tauri/binaries/*.exe
src-tauri/binaries/*.dll
src-tauri/gen/
*.log
.DS_Store
src-tauri/binaries/*-unknown-linux-gnu
*.bundle
whisper-diag.txt
setup-log.txt
# Updater signing keypair — the PRIVATE half must never be committed. It's
# delivered to you separately and only needs to live in GitHub's encrypted
# Actions secrets (see clipcaption/05-build-log.md for setup steps).
signing-key/
